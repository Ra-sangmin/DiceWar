using UnityEngine;
using UnityEngine.UI;

public class DiceSetPopup : MonoBehaviour
{
    [SerializeField] InputField easyMinInput;
	[SerializeField] InputField easyMaxInput;

	[SerializeField] InputField normalMinInput;
	[SerializeField] InputField normalMaxInput;

	[SerializeField] InputField hardMinInput;
	[SerializeField] InputField hardMaxInput;

	// Start is called once before the first execution of Update after the MonoBehaviour is created
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
